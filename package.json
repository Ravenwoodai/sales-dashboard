{
  "name": "sales-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local sales call intelligence dashboard for scheduled CSV transcript imports.",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
